# ─── private planning artefacts — local only ───
.planning/

# ─── OS / editor ───
.DS_Store
*.swp
.idea/
.vscode/

# ─── language build/runtime ───
# The compiled daemon binary (the CI e2e/release jobs build it as
# ./ocu-filestored; a local build must never enter the tree).
/ocu-filestored
*.bin
__pycache__/
*.pyc
.venv/
dist/
build/
.env
.env.*
!.env.example

# ─── local-only Claude Code instructions ───
CLAUDE.local.md
