// Module path reflects this public repo's broker module. Dependencies arrive
// with the first implementation PRs, each through the architecture repo's
// dependency policy (license gate + supply-chain gate); the scaffold is
// dependency-free.
module github.com/Wide-Moat/ocu-filestore

go 1.25.0
